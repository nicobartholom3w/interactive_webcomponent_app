import { PokeSuggestPipe } from './poke-suggest.pipe';

describe('PokeSuggestPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeSuggestPipe();
    expect(pipe).toBeTruthy();
  });
});
